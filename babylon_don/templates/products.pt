<!DOCTYPE html>
<html lang="us-en">
	<body>
		<div class="contents">
			<div tal:repeat="p products">
				<a href="${p.url}">buy ${p.title}</a>
				<a tal:condition="p.has_preview" href="${p.url}?try">preview product</a>
			</div>
		</div>
	</body>